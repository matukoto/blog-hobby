# 作業ログ  

**日付**: 2026-03-08  

## 依頼内容  

- 設計メモどおり、ブログコンテンツを `urara/**/+page.md` から  
  `src/lib/posts/*.md` へ移行する。  
- 旧ファイルは削除してよい。  

## 計画  

- Markdown ファイルをフラットな `src/lib/posts/` 構成へ移す。  
- `src/lib/posts.ts` の読み込み先を新パスへ切り替える。  
- ドキュメントを現行構成に合わせて更新し、check / test で検証する。  

## 実行内容  

- 15 件の投稿 Markdown を `src/lib/posts/<slug>.md` へ移動した。  
- `src/lib/posts.ts` の `import.meta.glob` と slug 抽出処理を  
  新しいファイル構造に合わせて更新した。  
- `.github/copilot-instructions.md` と  
  `.github/copilot-instructions.ja.md` を更新し、  
  コンテンツの配置先と textlint コマンドを新構成に合わせた。  

## 検証結果  

- `pnpm check` 成功  
- `pnpm test` 成功  
