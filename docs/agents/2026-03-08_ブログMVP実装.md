# 作業ログ  

**日付**: 2026-03-08  

## 依頼内容  

- `urara/**/+page.md` をデータソースに使いながら、  
  記事一覧 / 記事詳細 / タグ閲覧を持つブログ MVP を実装する。  
- t-wada 式 TDD を前提に進める。  
- 意味のある粒度でこまめにコミットする。  

## 計画  

- `src/lib/posts.ts` に共通のデータ取得層を作る。  
- `/articles/[slug]`、`/`、`/tags/[tag]` を順に実装する。  
- unit / component / E2E を通し、関連ドキュメントを更新する。  

## 実行内容  

- `src/lib/posts.ts` を実装し、`urara/**/+page.md` から frontmatter・本文・  
  タグ情報を読み取る共通ローダーを追加した。  
- `src/routes/+page.svelte` を記事一覧ページに差し替え、  
  `src/routes/articles/[slug]/` と `src/routes/tags/[tag]/` を実装した。  
- 記事画像の `/assets/...` パスで実際に表示できるように、  
  `static/assets/` へ必要な画像を配置した。  
- unit / component / E2E テストを追加・更新した。  
- 途中で 2 回コミットした。  
  - `Implement blog post data layer`  
  - `Implement blog routes and tags`  
- `.github/copilot-instructions.md` と  
  `.github/copilot-instructions.ja.md` を現状のブログ構成に合わせて更新した。  

## 検証結果  

- `pnpm check` 成功  
- `pnpm test` 成功  
- 追加したブログ導線（一覧 → 記事詳細 → タグ）を Playwright で確認  
